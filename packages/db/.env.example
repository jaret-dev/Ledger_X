# Postgres connection string used by Prisma.
# Local dev: spin up Postgres 15+ (e.g. `docker run -p 5432:5432 -e POSTGRES_PASSWORD=postgres postgres:15`)
# Railway: value is injected automatically by the Postgres plugin.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ledger?schema=public"
